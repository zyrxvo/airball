# Module airball.stars

The following documentation was automatically generated from the docstrings.

::: airball.stars.Star
::: airball.stars.Stars
    options:
      docstring_options:
        # Stars.__init__ accepts all its documented parameters via **kwargs
        # (m, b, v, inc, omega, Omega, UNIT_SYSTEM, size, environment).
        # Griffe cannot match **kwargs entries against the signature, so
        # this suppression is scoped narrowly here rather than globally.
        warn_unknown_params: false
